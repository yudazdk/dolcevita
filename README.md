This are the files for todo task.

The api folder contains index.php 
which gets requests from the clients 
and returns json data of the tasks in 
the database.

The sql structure is in the database folder.
